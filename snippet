
{% for item in site.data.navigation %}
          <a href="{{ item.link }}" {% if page.url == item.link %} style="color: red;"{% endif %}>
            {{ item.name }}
          </a>
          {% for sub_item in item.groups %}
          <a href="{{ item.link }}" {% if page.url == item.link %} style="color: red;"{% endif %}>
            {{ sub_item.name }}
          </a>
          
          <br>
        {% endfor %}
          <br>
        {% endfor %}