install:
  - bundle install --path vendor/bundle
  - npm ci

script: npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  local_dir: _site
  on:
    branch: master

